{
  "agent_name": "planner_multimodal_tool_agent",
  "max_replans": 2,
  "gpu_policy": {
    "sequential_loading": true,
    "cpu_offload": false,
    "cpu_fallback": false,
    "vram_budget_gib": 7.8
  },
  "city_profiles": {
    "nanjing": {
      "display_name": "南京",
      "climate_context": "夏热冬冷；夏季高温高辐射情景优先评价连续遮荫",
      "street_tree_policy": "连续高大乔木优先，幼树优先评估生长与养护，人工遮阳仅作补充",
      "leaf_off_months": [11, 12, 1, 2],
      "north_aligned_panorama": true,
      "panorama_width": 2048,
      "panorama_height": 512
    }
  },
  "tools": [
    {"name": "road_space_analysis", "role": "observe", "device": "cuda", "required": true},
    {"name": "phenology_review", "role": "verify", "device": "cuda", "required": true},
    {"name": "tree_gap_analysis", "role": "reason", "device": "cuda", "required": true},
    {"name": "directional_windows", "role": "localize", "device": "cuda", "required": true},
    {"name": "tree_layout", "role": "localize", "device": "cuda", "required": true},
    {"name": "structural_gate", "role": "verify", "device": "cuda", "required": true},
    {"name": "local_generation", "role": "generate", "device": "cuda", "required": false},
    {"name": "planning_overlay", "role": "render", "device": "cuda", "required": true},
    {"name": "generation_acceptance", "role": "verify", "device": "cuda", "required": true}
  ]
}
