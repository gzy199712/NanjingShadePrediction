# ThermalPrediction 技术流程

## 数据与模型

南京案例以 8,975 个街景点为基础。静态输入包括语义结构、DINOv2 全局特征和八方向局部特征；动态输入包括太阳几何、短波辐射、气温、湿度和风速。模型联合预测遮荫率与 Tmrt，再通过标准公式计算 UTCI。

训练采用点位和天气双重隔离、仅在训练集拟合标准化参数、验证集选择检查点、测试集一次性评估，并对五个随机种子模型做集成。对外入口依次运行训练、测试集评估、结果汇总和模型登记；消融实验是可选项。

## 遮荫路径导航

点位预测被映射到道路边，并按小时生成距离、遮荫、UTCI 和不确定性成本。路径引擎支持最短、遮荫优先、UTCI 优先和风险感知目标，使用带转向约束的 A* 搜索，并用确定性规则重新汇总路线指标。

FastAPI 只在本机监听，地名检索、路网、建筑与热成本均读取本地资料。用户查询和路线结果只保存在内存；语言模型不参与路径最优化。

## 街景优化

街景链先生成北向全景，再结合 Mask2Former、DINOv2、Depth Anything V2、Grounding DINO 和 Qwen3-VL 构建步行空间、现有树木、深度与道路证据。硬规则先排除机动车专用、步行空间可信度不足或现有遮荫充分的场景；其余场景生成树冠布局、候选掩膜和规划叠加图。

真实感候选必须通过掩膜内植被增长、掩膜外保真和语义一致性检查。未通过自动验收的图像不会作为正式规划结果。Image2 接口只负责准备请求材料和登记返回图像，不在仓库中保存服务凭据。

## 仓库边界

源码仓库包含工作流、模型与路由实现、街景规划代码、配置和文档。原始街景、GIS 数据、训练数组、模型权重、路由成本、日志、检查点、生成图和软著申请材料均在仓库外保存，并通过相对路径接入。
